# 飞书工作区与群聊双向协同桥接插件
- insert:
    - id: feishu-workspace-bridge
      name: 'dsh-plugin-feishu-workspace'
      inject: [tools, webServer, sessionController]
      config:
        databasePath: !!js process.env.FEISHU_WORKSPACE_BRIDGE_DB || ''
