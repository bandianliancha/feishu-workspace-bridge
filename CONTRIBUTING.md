# Contributing

感谢参与 Feishu Workspace Bridge。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 安装依赖：`npm install`。
3. 保持插件只围绕机器人绑定、工作区群生命周期和消息双向同步。
4. 为可靠性或行为变更补充有意义的测试。
5. 运行 `npm run check`。
6. 提交 Pull Request，说明触发条件、修改后的行为和验证方式。

不要提交真实 App ID、App Secret、Open ID、chat_id、tenant token、DSH 数据库或日志中的个人数据。
